name: georgia-mgwr-reproduction

on:
  push:
    branches: [main]
    paths:
      - "experiments/real_data/georgia_mgwr_2019/**"
      - ".github/workflows/georgia-mgwr-reproduction.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  reproduce:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install pinned reproduction environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install \
            "numpy==1.26.4" \
            "scipy==1.11.4" \
            "pandas==2.2.3" \
            "libpysal==4.10.0" \
            "mgwr==2.2.1"

      - name: Reproduce Oshan et al. Georgia example
        run: |
          python experiments/real_data/georgia_mgwr_2019/reproduce.py --vendor-data

      - name: Show result summary
        run: cat results/reproduction/georgia_mgwr_2019/README.md

      - name: Persist canonical data and reproduction outputs
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/raw/georgia
          git add -f results/reproduction/georgia_mgwr_2019
          if git diff --cached --quiet; then
            echo "No generated changes to commit."
          else
            git commit -m "data: vendor Georgia and store MGWR reproduction [skip ci]"
            git push
          fi
